# Contracts

This feature does not expose network endpoints. Navigation is performed locally in the
TUI. The `openapi.yaml` file is provided to explicitly document the absence of HTTP
contracts for this feature.
